# Code generated by gen-crd-schemas. DO NOT EDIT.

components:
  schemas:
    Backup:
      description: Backup is the Schema for the backups API
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: BackupSpec defines the desired state of Backup
          properties:
            backupClassName:
              description: BackupClassName is the backup tool to use for the backup.
              type: string
            config:
              description: |-
                Config defines the configuration for the backup job.
                These options are specific to the BackupClass being used and must conform to
                the schema defined in the BackupClass's .spec.config.openAPIV3Schema.
              type: object
              x-kubernetes-preserve-unknown-fields: true
            destination:
              description: Destination is the destination for the backup data.
              properties:
                backupStorageName:
                  description: BackupStorageName is the name of the BackupStorage
                    to use for the backup.
                  type: string
                s3:
                  description: S3 contains the S3 information for the backup destination.
                  properties:
                    accessKeyId:
                      description: |-
                        AccessKeyID allows specifying the S3 access key ID inline.
                        It is provided as a write-only input field for convenience.
                        When this field is set, a webhook writes this value in the Secret specified by `credentialsSecretName`
                        and empties this field.
                        This field is not stored in the API.
                      type: string
                    bucket:
                      description: Bucket is the name of the S3 bucket.
                      type: string
                    credentialsSecretName:
                      description: |-
                        CredentialsSecreName is the reference to the secret containing the S3 credentials.
                        The Secret must contain the keys `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY`.
                      type: string
                    endpointURL:
                      description: EndpointURL is an endpoint URL of backup storage.
                      type: string
                      x-kubernetes-validations:
                      - message: endpointURL must be a valid URL
                        rule: isURL(self)
                    forcePathStyle:
                      default: false
                      description: |-
                        ForcePathStyle is set to use path-style URLs.
                        If unspecified, the default value is false.
                      type: boolean
                    region:
                      description: Region is the region of the S3 bucket.
                      type: string
                    secretAccessKey:
                      description: |-
                        SecretAccessKey allows specifying the S3 secret access key inline.
                        It is provided as a write-only input field for convenience.
                        When this field is set, a webhook writes this value in the Secret specified by `credentialsSecretName`
                        and empties this field.
                        This field is not stored in the API.
                      type: string
                    verifyTLS:
                      default: true
                      description: |-
                        VerifyTLS is set to ensure TLS/SSL verification.
                        If unspecified, the default value is true.
                      type: boolean
                  required:
                  - bucket
                  - credentialsSecretName
                  - endpointURL
                  - region
                  type: object
              type: object
            instanceName:
              description: InstanceName is the name of the Instance to back up.
              type: string
          required:
          - backupClassName
          - destination
          - instanceName
          type: object
        status:
          description: BackupStatus defines the observed state of Backup.
          properties:
            completedAt:
              description: CompletedAt is the time when the backup job completed successfully.
              format: date-time
              type: string
            conditions:
              items:
                description: Condition contains details for one aspect of the current
                  state of this API Resource.
                properties:
                  lastTransitionTime:
                    description: |-
                      lastTransitionTime is the last time the condition transitioned from one status to another.
                      This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                    format: date-time
                    type: string
                  message:
                    description: |-
                      message is a human readable message indicating details about the transition.
                      This may be an empty string.
                    maxLength: 32768
                    type: string
                  observedGeneration:
                    description: |-
                      observedGeneration represents the .metadata.generation that the condition was set based upon.
                      For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                      with respect to the current state of the instance.
                    format: int64
                    minimum: 0
                    type: integer
                  reason:
                    description: |-
                      reason contains a programmatic identifier indicating the reason for the condition's last transition.
                      Producers of specific condition types may define expected values and meanings for this field,
                      and whether the values are considered a guaranteed API.
                      The value should be a CamelCase string.
                      This field may not be empty.
                    maxLength: 1024
                    minLength: 1
                    pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                    type: string
                  status:
                    description: status of the condition, one of True, False, Unknown.
                    enum:
                    - "True"
                    - "False"
                    - Unknown
                    type: string
                  type:
                    description: type of condition in CamelCase or in foo.example.com/CamelCase.
                    maxLength: 316
                    pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                    type: string
                required:
                - lastTransitionTime
                - message
                - reason
                - status
                - type
                type: object
              type: array
              x-kubernetes-list-map-keys:
              - type
              x-kubernetes-list-type: map
            jobName:
              description: JobName is the reference to the job that is running the
                backup.
              type: string
            lastObservedGeneration:
              description: LastObservedGeneration is the last observed generation
                of the backup job.
              format: int64
              type: integer
            message:
              description: Message is the message of the backup job.
              type: string
            startedAt:
              description: StartedAt is the time when the backup job started.
              format: date-time
              type: string
            state:
              description: State is the current state of the backup job.
              type: string
          type: object
      required:
      - spec
      type: object
    BackupClass:
      description: BackupClass is the Schema for the backupclasses API
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: BackupClassSpec defines the desired state of BackupClass
          properties:
            cleanupJobSpec:
              description: CleanupJobSpec is the specification of the cleanup job.
              properties:
                command:
                  description: Command is the command to run the backup tool.
                  items:
                    type: string
                  type: array
                image:
                  description: Image is the image of the backup tool.
                  type: string
              type: object
            clusterPermissions:
              description: |-
                ClusterPermissions defines the cluster-wide permissions required by the backup tool.
                These permissions are used to generate a ClusterRole for the backup job.
              items:
                description: |-
                  PolicyRule holds information that describes a policy rule, but does not contain information
                  about who the rule applies to or which namespace the rule applies to.
                properties:
                  apiGroups:
                    description: |-
                      APIGroups is the name of the APIGroup that contains the resources.  If multiple API groups are specified, any action requested against one of
                      the enumerated resources in any API group will be allowed. "" represents the core API group and "*" represents all API groups.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  nonResourceURLs:
                    description: |-
                      NonResourceURLs is a set of partial urls that a user should have access to.  *s are allowed, but only as the full, final step in the path
                      Since non-resource URLs are not namespaced, this field is only applicable for ClusterRoles referenced from a ClusterRoleBinding.
                      Rules can either apply to API resources (such as "pods" or "secrets") or non-resource URL paths (such as "/api"),  but not both.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  resourceNames:
                    description: ResourceNames is an optional white list of names
                      that the rule applies to.  An empty set means that everything
                      is allowed.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  resources:
                    description: Resources is a list of resources this rule applies
                      to. '*' represents all resources.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  verbs:
                    description: Verbs is a list of Verbs that apply to ALL the ResourceKinds
                      contained in this rule. '*' represents all verbs.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                required:
                - verbs
                type: object
              type: array
            config:
              description: Config contains additional configuration defined for the
                backup tool.
              properties:
                openAPIV3Schema:
                  description: OpenAPIV3Schema is the OpenAPI v3 schema of the backup
                    tool.
                  x-kubernetes-preserve-unknown-fields: true
              type: object
            dataStoreConstraints:
              description: |-
                DataStoreConstraints defines compatibility requirements and prerequisites that must be satisfied
                by a DataStore before this backup tool can be used with it. This allows the backup tool to
                express specific requirements about the database configuration needed for successful backup operations,
                such as required database fields, specific engine configurations, or other database properties.
                When a DataStore references this backup tool, the operator will validate the DataStore
                against these constraints before proceeding with the backup operation.
              properties:
                requiredFields:
                  description: |-
                    RequiredFields contains a list of fields that must be set in the DataStore spec.
                    Each key is a JSON path expressions that points to a field in the DataStore spec.
                    For example, ".spec.engine.type" or ".spec.dataSource.dataImport.config.someField".
                  items:
                    type: string
                  type: array
              type: object
            description:
              description: Description is the description of the backup tool.
              type: string
            displayName:
              type: string
            jobSpec:
              description: JobSpec is the specification of the backup job.
              properties:
                command:
                  description: Command is the command to run the backup tool.
                  items:
                    type: string
                  type: array
                image:
                  description: Image is the image of the backup tool.
                  type: string
              type: object
            permissions:
              description: |-
                Permissions defines the permissions required by the backup tool.
                These permissions are used to generate a Role for the backup job.
              items:
                description: |-
                  PolicyRule holds information that describes a policy rule, but does not contain information
                  about who the rule applies to or which namespace the rule applies to.
                properties:
                  apiGroups:
                    description: |-
                      APIGroups is the name of the APIGroup that contains the resources.  If multiple API groups are specified, any action requested against one of
                      the enumerated resources in any API group will be allowed. "" represents the core API group and "*" represents all API groups.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  nonResourceURLs:
                    description: |-
                      NonResourceURLs is a set of partial urls that a user should have access to.  *s are allowed, but only as the full, final step in the path
                      Since non-resource URLs are not namespaced, this field is only applicable for ClusterRoles referenced from a ClusterRoleBinding.
                      Rules can either apply to API resources (such as "pods" or "secrets") or non-resource URL paths (such as "/api"),  but not both.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  resourceNames:
                    description: ResourceNames is an optional white list of names
                      that the rule applies to.  An empty set means that everything
                      is allowed.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  resources:
                    description: Resources is a list of resources this rule applies
                      to. '*' represents all resources.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  verbs:
                    description: Verbs is a list of Verbs that apply to ALL the ResourceKinds
                      contained in this rule. '*' represents all verbs.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                required:
                - verbs
                type: object
              type: array
            supportedProviders:
              description: SupportedProviders is the list of providers that the backup
                tool supports.
              items:
                type: string
              type: array
          type: object
        status:
          description: BackupClassStatus defines the observed state of BackupClass.
          properties:
            conditions:
              items:
                description: Condition contains details for one aspect of the current
                  state of this API Resource.
                properties:
                  lastTransitionTime:
                    description: |-
                      lastTransitionTime is the last time the condition transitioned from one status to another.
                      This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                    format: date-time
                    type: string
                  message:
                    description: |-
                      message is a human readable message indicating details about the transition.
                      This may be an empty string.
                    maxLength: 32768
                    type: string
                  observedGeneration:
                    description: |-
                      observedGeneration represents the .metadata.generation that the condition was set based upon.
                      For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                      with respect to the current state of the instance.
                    format: int64
                    minimum: 0
                    type: integer
                  reason:
                    description: |-
                      reason contains a programmatic identifier indicating the reason for the condition's last transition.
                      Producers of specific condition types may define expected values and meanings for this field,
                      and whether the values are considered a guaranteed API.
                      The value should be a CamelCase string.
                      This field may not be empty.
                    maxLength: 1024
                    minLength: 1
                    pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                    type: string
                  status:
                    description: status of the condition, one of True, False, Unknown.
                    enum:
                    - "True"
                    - "False"
                    - Unknown
                    type: string
                  type:
                    description: type of condition in CamelCase or in foo.example.com/CamelCase.
                    maxLength: 316
                    pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                    type: string
                required:
                - lastTransitionTime
                - message
                - reason
                - status
                - type
                type: object
              type: array
              x-kubernetes-list-map-keys:
              - type
              x-kubernetes-list-type: map
          type: object
      required:
      - spec
      type: object
    BackupClassList:
      description: BackupClassList is an object that contains the list of the existing
        backupclasss.
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        items:
          items:
            $ref: '#/components/schemas/BackupClass'
          type: array
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          properties:
            name:
              description: 'Name must be unique within a namespace. Is required when
                creating resources, although some resources may allow a client to
                request the generation of an appropriate name automatically. Name
                is primarily intended for creation idempotence and configuration definition.
                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names'
              type: string
          type: object
      type: object
    BackupList:
      description: BackupList is an object that contains the list of the existing
        backups.
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        items:
          items:
            $ref: '#/components/schemas/Backup'
          type: array
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          properties:
            name:
              description: 'Name must be unique within a namespace. Is required when
                creating resources, although some resources may allow a client to
                request the generation of an appropriate name automatically. Name
                is primarily intended for creation idempotence and configuration definition.
                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names'
              type: string
            namespace:
              description: 'Namespace defines the space within which each name must
                be unique. An empty namespace is equivalent to the "default" namespace,
                but "default" is the canonical representation. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces'
              type: string
          type: object
      type: object
    Instance:
      description: Instance is the Schema for the instances API
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: InstanceSpec defines the desired state of Instance
          properties:
            components:
              additionalProperties:
                properties:
                  config:
                    description: Config specifies the component specific configuration.
                    properties:
                      configMapRef:
                        description: |-
                          LocalObjectReference contains enough information to let you locate the
                          referenced object inside the same namespace.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      key:
                        type: string
                      secretRef:
                        description: |-
                          LocalObjectReference contains enough information to let you locate the
                          referenced object inside the same namespace.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  customSpec:
                    description: |-
                      CustomSpec provides an API for customising this component.
                      The API schema is defined by the provider's ComponentSchemas.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  image:
                    description: |-
                      Image specifies an override for the image to use.
                      When unspecified, it is autmatically set from the ComponentVersions
                      based on the Version specified.
                    type: string
                  name:
                    description: Name of the component.
                    type: string
                  replicas:
                    description: Replicas specifies the number of replicas for this
                      component.
                    format: int32
                    type: integer
                  resources:
                    description: Resources requirements for this component.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  storage:
                    description: |-
                      Storage requirements for this component.
                      For stateless components, this is an optional field.
                    properties:
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClass:
                        type: string
                    type: object
                  type:
                    description: Type of the component from the Provider.
                    type: string
                  version:
                    description: Version of the component from ComponentVersions.
                    type: string
                type: object
              description: |-
                Components defines the component instances for this cluster.
                The keys are component names (e.g., "engine", "proxy", "backupAgent").
                Which components are valid depends on the selected topology.
              type: object
            global:
              description: |-
                Global contains provider-level configuration that applies to the entire cluster.
                The schema for this field is defined by the provider's GlobalConfigSchema.
              type: object
              x-kubernetes-preserve-unknown-fields: true
            provider:
              description: Provider is the name of the database provider (e.g., "psmdb",
                "postgresql").
              type: string
            topology:
              description: Topology defines the deployment topology and its configuration.
              properties:
                config:
                  description: |-
                    Config contains topology-specific configuration.
                    The schema for this field is defined by the provider's TopologyDefinition.
                    Examples: shard count for sharded topology, replication factor, etc.
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                type:
                  description: |-
                    Type is the topology name (e.g., "sharded", "replicaset").
                    The available topologies are defined by the provider.
                    If omitted, the provider's default topology is used.
                  type: string
              type: object
            version:
              description: |-
                Version selects a provider-defined version bundle, resolving compatible
                versions for all components automatically. Per-component versions set
                in Components take precedence over the bundle.
                If omitted and the provider defines a default bundle, that bundle is used.
              type: string
          type: object
        status:
          description: InstanceStatus defines the observed state of Instance.
          properties:
            components:
              description: Components is the status of the components in the database
                cluster.
              items:
                properties:
                  pods:
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  ready:
                    format: int32
                    type: integer
                  state:
                    type: string
                  total:
                    format: int32
                    type: integer
                type: object
              type: array
            conditions:
              items:
                description: Condition contains details for one aspect of the current
                  state of this API Resource.
                properties:
                  lastTransitionTime:
                    description: |-
                      lastTransitionTime is the last time the condition transitioned from one status to another.
                      This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                    format: date-time
                    type: string
                  message:
                    description: |-
                      message is a human readable message indicating details about the transition.
                      This may be an empty string.
                    maxLength: 32768
                    type: string
                  observedGeneration:
                    description: |-
                      observedGeneration represents the .metadata.generation that the condition was set based upon.
                      For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                      with respect to the current state of the instance.
                    format: int64
                    minimum: 0
                    type: integer
                  reason:
                    description: |-
                      reason contains a programmatic identifier indicating the reason for the condition's last transition.
                      Producers of specific condition types may define expected values and meanings for this field,
                      and whether the values are considered a guaranteed API.
                      The value should be a CamelCase string.
                      This field may not be empty.
                    maxLength: 1024
                    minLength: 1
                    pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                    type: string
                  status:
                    description: status of the condition, one of True, False, Unknown.
                    enum:
                    - "True"
                    - "False"
                    - Unknown
                    type: string
                  type:
                    description: type of condition in CamelCase or in foo.example.com/CamelCase.
                    maxLength: 316
                    pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                    type: string
                required:
                - lastTransitionTime
                - message
                - reason
                - status
                - type
                type: object
              type: array
              x-kubernetes-list-map-keys:
              - type
              x-kubernetes-list-type: map
            connectionSecretRef:
              description: |-
                ConnectionSecretRef is a reference to the Secret containing connection details.
                The Secret is auto-generated by the provider-runtime reconciler with the name
                "{instance-name}-conn" and owned by the Instance (auto-deleted on cleanup).

                The Secret uses well-known keys inspired by the Service Binding specification:
                  - "type"     - Database type (e.g., "mongodb", "postgresql")
                  - "provider" - Provider name (e.g., "percona-server-mongodb")
                  - "host"     - Hostname or IP address
                  - "port"     - Port number
                  - "username" - Database username
                  - "password" - Database password
                  - "uri"      - Full connection URI including credentials
              properties:
                name:
                  default: ""
                  description: |-
                    Name of the referent.
                    This field is effectively required, but due to backwards compatibility is
                    allowed to be empty. Instances of this type with an empty value here are
                    almost certainly wrong.
                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                  type: string
              type: object
              x-kubernetes-map-type: atomic
            phase:
              description: Phase of the database cluster.
              enum:
              - Pending
              - Provisioning
              - Initializing
              - Ready
              - Updating
              - Terminating
              - Failed
              - Restoring
              - Suspending
              - Suspended
              - Resuming
              type: string
            version:
              description: |-
                Version is the effective version bundle that is currently applied to this
                Instance. On the first reconciliation the provider-runtime writes the
                resolved default bundle name here and uses this value on every subsequent
                reconciliation when spec.version is empty. This ensures that a Provider
                upgrade (which may change the default bundle) never silently triggers an
                unintended database upgrade on existing Instances.

                GitOps tools (ArgoCD, Flux) exclude status from diff calculations by
                default, so this field does not cause spurious out-of-sync alerts.
              type: string
          type: object
      required:
      - spec
      type: object
    InstanceConnectionDetails:
      additionalProperties:
        description: AdditionalProperties holds additional provider-specific connection
          details
        type: string
      description: ConnectionDetails holds the typed connection details for a database
        instance. These are written by the provider-runtime reconciler to a Kubernetes
        Secret and later read back by the API server to serve the connection endpoint.
        They follow the Service Binding well-known keys where applicable.
      properties:
        host:
          description: Host is the hostname or IP address to connect to
          type: string
        password:
          description: Password is the password for authentication
          type: string
        port:
          description: Port is the port number to connect to
          type: string
        provider:
          description: Provider is the provider that manages this instance
          type: string
        type:
          description: Type is the type of database (e.g., mongodb, postgresql, mysql)
          type: string
        uri:
          description: URI is a pre-built connection URI
          type: string
        username:
          description: Username is the username for authentication
          type: string
      type: object
    InstanceList:
      description: InstanceList is an object that contains the list of the existing
        instances.
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        items:
          items:
            $ref: '#/components/schemas/Instance'
          type: array
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          properties:
            name:
              description: 'Name must be unique within a namespace. Is required when
                creating resources, although some resources may allow a client to
                request the generation of an appropriate name automatically. Name
                is primarily intended for creation idempotence and configuration definition.
                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names'
              type: string
            namespace:
              description: 'Namespace defines the space within which each name must
                be unique. An empty namespace is equivalent to the "default" namespace,
                but "default" is the canonical representation. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces'
              type: string
          type: object
      type: object
    MonitoringConfig:
      description: MonitoringConfig is the Schema for the monitoringconfigs API.
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: spec defines the desired state of MonitoringConfig
          properties:
            credentialsSecretName:
              description: |-
                CredentialsSecretName is the reference to the secret containing the API key.
                It contains `apiKey` key with the API key value.
              type: string
            pmm:
              description: PMM is configuration specific for monitoring using PMM
                tool.
              properties:
                url:
                  description: URL is url to the monitoring config.
                  type: string
              required:
              - url
              type: object
            type:
              description: Type is the name of monitoring tool (e.g., "pmm").
              enum:
              - pmm
              type: string
            verifyTLS:
              default: true
              description: |-
                VerifyTLS is set to ensure TLS/SSL verification.
                If unspecified, the default value is true.
              type: boolean
          required:
          - credentialsSecretName
          - type
          type: object
        status:
          default:
            inUse: false
          description: status defines the observed state of MonitoringConfig
          properties:
            inUse:
              default: false
              description: InUse is a flag that indicates if any Instance uses the
                monitoring config.
              type: boolean
            lastObservedGeneration:
              description: LastObservedGeneration is the most recent generation observed
                for this MonitoringConfig.
              format: int64
              type: integer
            pmmServerVersion:
              description: PMMServerVersion shows PMM server version.
              type: string
          type: object
      required:
      - spec
      type: object
    MonitoringConfigList:
      description: MonitoringConfigList is an object that contains the list of the
        existing monitoringconfigs.
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        items:
          items:
            $ref: '#/components/schemas/MonitoringConfig'
          type: array
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          properties:
            name:
              description: 'Name must be unique within a namespace. Is required when
                creating resources, although some resources may allow a client to
                request the generation of an appropriate name automatically. Name
                is primarily intended for creation idempotence and configuration definition.
                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names'
              type: string
            namespace:
              description: 'Namespace defines the space within which each name must
                be unique. An empty namespace is equivalent to the "default" namespace,
                but "default" is the canonical representation. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces'
              type: string
          type: object
      type: object
    Provider:
      description: Provider is the Schema for the providers API
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: ProviderSpec defines the desired state of Provider
          properties:
            componentTypes:
              additionalProperties:
                properties:
                  versions:
                    items:
                      properties:
                        default:
                          type: boolean
                        image:
                          type: string
                        version:
                          type: string
                      type: object
                    type: array
                type: object
              type: object
            components:
              additionalProperties:
                properties:
                  customSpecSchema:
                    description: CustomSpecSchema holds the OpenAPI v3 schema for
                      this component's CustomSpec.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  type:
                    type: string
                type: object
              type: object
            globalConfigSchema:
              description: GlobalConfigSchema holds the OpenAPI v3 schema for the
                global configuration.
              type: object
              x-kubernetes-preserve-unknown-fields: true
            topologies:
              additionalProperties:
                properties:
                  components:
                    additionalProperties:
                      properties:
                        optional:
                          type: boolean
                      type: object
                    type: object
                  configSchema:
                    description: ConfigSchema holds the OpenAPI v3 schema for topology-specific
                      configuration.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              type: object
            uiSchema:
              description: UISchema holds the UI rendering hints for each topology.
              type: object
              x-kubernetes-preserve-unknown-fields: true
            versions:
              description: |-
                Versions defines curated version bundles — named sets of component
                versions that are known to be mutually compatible. Users reference
                a bundle via Instance.Spec.Version. If the user does not set a version,
                the bundle whose Default field is true is used automatically.
              items:
                description: |-
                  VersionBundle is a curated set of component versions known to be mutually
                  compatible. Provider developers define bundles in definition/versions.yaml.
                properties:
                  components:
                    additionalProperties:
                      type: string
                    description: |-
                      Components maps component names to their version strings for this bundle.
                      Keys must match component names defined in ProviderSpec.Components.
                    type: object
                  default:
                    description: |-
                      Default marks this bundle as the implicit choice when an Instance omits
                      Spec.Version entirely. Exactly one bundle should have Default: true.
                    type: boolean
                  name:
                    description: |-
                      Name is the unique identifier for this bundle (e.g. "8.0.12").
                      Users set Instance.Spec.Version to this value to select the bundle.
                    type: string
                required:
                - name
                type: object
              type: array
          type: object
        status:
          description: ProviderStatus defines the observed state of Provider.
          properties:
            conditions:
              items:
                description: Condition contains details for one aspect of the current
                  state of this API Resource.
                properties:
                  lastTransitionTime:
                    description: |-
                      lastTransitionTime is the last time the condition transitioned from one status to another.
                      This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                    format: date-time
                    type: string
                  message:
                    description: |-
                      message is a human readable message indicating details about the transition.
                      This may be an empty string.
                    maxLength: 32768
                    type: string
                  observedGeneration:
                    description: |-
                      observedGeneration represents the .metadata.generation that the condition was set based upon.
                      For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                      with respect to the current state of the instance.
                    format: int64
                    minimum: 0
                    type: integer
                  reason:
                    description: |-
                      reason contains a programmatic identifier indicating the reason for the condition's last transition.
                      Producers of specific condition types may define expected values and meanings for this field,
                      and whether the values are considered a guaranteed API.
                      The value should be a CamelCase string.
                      This field may not be empty.
                    maxLength: 1024
                    minLength: 1
                    pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                    type: string
                  status:
                    description: status of the condition, one of True, False, Unknown.
                    enum:
                    - "True"
                    - "False"
                    - Unknown
                    type: string
                  type:
                    description: type of condition in CamelCase or in foo.example.com/CamelCase.
                    maxLength: 316
                    pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                    type: string
                required:
                - lastTransitionTime
                - message
                - reason
                - status
                - type
                type: object
              type: array
              x-kubernetes-list-map-keys:
              - type
              x-kubernetes-list-type: map
          type: object
      required:
      - spec
      type: object
    ProviderList:
      description: ProviderList is an object that contains the list of the existing
        providers.
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        items:
          items:
            $ref: '#/components/schemas/Provider'
          type: array
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          properties:
            name:
              description: 'Name must be unique within a namespace. Is required when
                creating resources, although some resources may allow a client to
                request the generation of an appropriate name automatically. Name
                is primarily intended for creation idempotence and configuration definition.
                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names'
              type: string
          type: object
      type: object
info:
  description: OpenAPI schemas extracted from Kubernetes CRD manifests. This file
    is auto-generated - do not edit manually.
  title: OpenEverest CRD Schemas
  version: 1.0.0
openapi: 3.0.2
paths: null
